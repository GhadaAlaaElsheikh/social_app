//import {v4 as uuidv4} from 'uuid'
//console.log({otp:uuidv4().replaceAll(/\D/g,"").slice(0,6)});

//console.log(Math.floor(Math.random()*(99999-10000+1)+10000));

import bootstrap from "./app.controller"
 
 bootstrap()